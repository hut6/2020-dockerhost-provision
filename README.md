# 2020-dockerhost-provision

```
> git clone https://github.com/hut6/2020-dockerhost-provision.git
> cd 2020-dockerhost-provision/
> cp .env.dist .env
> vi .env #setup node hostname and loki/cortex urls
> ./install-docker.sh
> ./deploy-stack.sh
```
